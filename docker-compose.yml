version: '3.8'

services:
  web:
    build: .
    container_name: adonai_dashboard
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=False
      - PORT=5000
      - HOST=0.0.0.0
      - SECRET_KEY=adonai_produccion_secreta_!2026
    volumes:
      - ./data:/app/data
