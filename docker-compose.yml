version: '3.8'

services:
  web:
    build: .
    container_name: adonai_dashboard
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - /opt/adonai/data:/app/data
